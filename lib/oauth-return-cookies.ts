/** Cookie dự phòng khi redirectTo chỉ còn `/auth/callback` (không query) để khớp Redirect URLs trên Supabase. */
export const OAUTH_NEXT_COOKIE = "anthichtuhoc_oauth_next";
export const OAUTH_POPUP_COOKIE = "anthichtuhoc_oauth_popup";
